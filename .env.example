# ProtoFaker Release Environment Variables Template
# ================================================
#
# This file contains template environment variables required for JReleaser-based releases.
# 
# SECURITY WARNING: NEVER commit actual credentials to version control!
# 
# Instructions:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Fill in your actual credentials in .env.local
# 3. Source the variables before release: source .env.local
# 4. Verify .env.local is in .gitignore (it should be)

# GitHub Release Configuration
# ----------------------------
# Personal Access Token with repo permissions for creating GitHub releases
# Generate at: https://github.com/settings/tokens
JRELEASER_GITHUB_TOKEN="your-github-token-here"

# Maven Central Portal Configuration  
# ----------------------------------
# New Maven Central Portal credentials (replaces legacy OSSRH)
# Register at: https://central.sonatype.com/
JRELEASER_MAVENCENTRAL_USERNAME="your-portal-username"
JRELEASER_MAVENCENTRAL_PASSWORD="your-portal-token"

# GPG Signing Configuration
# -------------------------
# GPG key used for signing artifacts (RSA 4096-bit key 0BF66CC2B921A8AA)
# Export public key: gpg --armor --export 0BF66CC2B921A8AA | base64 -w 0
# Export private key: gpg --armor --export-secret-keys 0BF66CC2B921A8AA | base64 -w 0
JRELEASER_GPG_PUBLIC_KEY="base64-encoded-public-key-here"
JRELEASER_GPG_SECRET_KEY="base64-encoded-private-key-here"

# GPG passphrase (leave empty if key has no passphrase)
JRELEASER_GPG_PASSPHRASE=""

# Optional: Signing Key ID (if not using default key)
# JRELEASER_GPG_KEY_NAME="0BF66CC2B921A8AA"

# Development and Testing
# -----------------------
# Set to true for dry-run releases (testing configuration)
# JRELEASER_DRY_RUN="true"

# Set logging level for JReleaser (DEBUG, INFO, WARN, ERROR)
# JRELEASER_LOG_LEVEL="INFO"

# Usage Examples:
# ===============
# 
# Test configuration (dry run):
# source .env.local && ./gradlew jreleaserRelease --dryrun
#
# Full production release:
# source .env.local && ./gradlew jreleaserFullRelease
#
# Test configuration only:
# source .env.local && ./gradlew jreleaserConfig
#
# Validate GPG setup:
# echo "$JRELEASER_GPG_PUBLIC_KEY" | base64 -d | gpg --import --dry-run

# Security Best Practices:
# ========================
# - Rotate tokens and keys regularly
# - Use least-privilege access for tokens
# - Store credentials in secure password managers
# - Never share credentials via email or chat
# - Monitor for credential exposure in logs
# - Use environment-specific credentials (dev/staging/prod)